# JitPack builds with its own JDK, and compiling against Paper 26.2 needs 25. Gradle
# fetches the right one itself — that is what foojay-resolver in settings.gradle.kts is for.
jdk:
  - openjdk21
before_install:
  - chmod +x gradlew
