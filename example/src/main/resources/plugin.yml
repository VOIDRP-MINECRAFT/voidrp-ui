name: UiDemo
version: '${version}'
main: com.example.uidemo.UiDemoPlugin
api-version: '1.21'
# VoidRP UI draws the pages; without it this plugin has nothing to do.
depend: [VoidRpUI]
commands:
  uidemo:
    description: Open the example page
