// Отдельный проект: эту папку можно скопировать куда угодно и собрать саму по себе.
plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "voidrp-ui-example"
